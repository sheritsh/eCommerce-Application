import CategoryBreadcrumb from '../Breadcrumbs/CategoryBreadcrumb';

const customRoutes = [{ path: '/categories/:categoryId', breadcrumb: CategoryBreadcrumb } as object];

export default customRoutes;
